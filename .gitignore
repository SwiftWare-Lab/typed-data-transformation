# ignore all files beginning with “layers.”
layers.*
# Remove any currently tracked layers.* files from the index
git rm --cached layers.*      

# Add your .gitignore (in case you haven’t committed it yet)
git add .gitignore            

# Commit the change
git commit -m "Ignore all layers.* files"
